# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
hf_cache

gpt-2/ 

# Virtual environments
venv/
.venv/
.venv_local/
env/
.env

# Local debugging scratch outputs (keep .py, ignore generated logs)
scratch/*_out.txt
scratch/bf16_test.py
scratch/depth_test.py
scratch/fix_test.py
scratch/final_validate.py

*.pt

# Large datasets / caches
.hf_cache/
wandb/
live_bench/
livebench_coding/downloads/
livebench_coding/test/*.arrow
nanoGPT/data/
EBT/data/
data/
**/*.bin

# Python build artifacts
*.egg-info/
dist/
build/

# OS-specific files
.DS_Store

# Project artifacts
out.txt
out/
out_ebt/
logs/
checkpoints/
runs/
**/*.ckpt
**/*.pt
Miniconda3-latest-MacOSX-arm64.sh
*.tar.gz
*.zip

# Cloned-on-demand on the GPU box by setup_gpu.sh / setup_gpu_blackwell.sh
# (not tracked here: keeps pushes fast, avoids submodule churn).
/EBT/
/nanochat/

# Microsoft Word/Excel lock files
~$*

# Local SSH helpers / probes (may contain credentials) — never commit
scratch/*.exp
scratch/uni_*.txt
scratch/*.txt
scratch/*.log
