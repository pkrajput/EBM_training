# Blackwell (B200 / RTX PRO 6000 / RTX 5090) requirements.
#
# Blackwell architecture (sm_100, sm_120) is NOT supported by the CUDA 12.1
# wheels that EBT pins. We need torch >= 2.7 built against CUDA 12.8+.
#
# The pins below are looser than the Hopper file because Blackwell wheels
# move faster and we don't want to fight resolution on a fresh VM.
--extra-index-url https://download.pytorch.org/whl/cu128

# Core runtime (PyTorch 2.7+ has Blackwell support via cu128 wheels)
torch==2.7.1
torchvision==0.22.1
triton>=3.2

# Lightning stack (we only need pl.LightningModule as a base class)
pytorch-lightning>=2.5
torchmetrics>=1.5

# HF stack
transformers>=4.46
tokenizers>=0.20
huggingface-hub>=0.26
datasets>=3.0
diffusers>=0.31              # needed at module load time by EBT/model_utils
safetensors>=0.4
accelerate>=1.0
tiktoken>=0.12
sentencepiece>=0.2
protobuf>=4.25
regex>=2024.9

# Data / numerics
numpy>=2.0,<3
pandas>=2.2
pyarrow>=17
scipy>=1.13
sympy>=1.13
mpmath>=1.3
networkx>=3.2
pillow>=10
opencv-python-headless>=4.10
matplotlib>=3.9
nltk>=3.9
timm>=1.0

# Eval / CORE bundle / HumanEval sandbox
jinja2>=3.1
PyYAML>=6.0
filelock>=3.16
xxhash>=3.5

# Networking / utilities
requests>=2.32
urllib3>=2.2
certifi>=2024.8
charset-normalizer>=3.3
idna>=3.8
aiohttp>=3.10
attrs>=24
click>=8.1
fsspec>=2024.6
packaging>=24
platformdirs>=4.3
psutil>=6.0
tqdm>=4.66
dill>=0.3.8
multiprocess>=0.70

# Experiment tracking
wandb>=0.18
GitPython>=3.1

# Stdlib helpers
python-dateutil>=2.9
pytz>=2024
typing_extensions>=4.12
